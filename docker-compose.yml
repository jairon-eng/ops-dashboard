services:
  db:
    image: postgres:16
    container_name: ops-dashboard-db
    environment:
      POSTGRES_DB: opsdashboard
      POSTGRES_USER: opsuser
      POSTGRES_PASSWORD: opspass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
